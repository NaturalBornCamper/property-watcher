@AGENTS.md

# CLAUDE.md

`AGENTS.md` (imported above) is the source of truth for this repository. It is provider-neutral and read by every agent that works here.

This file exists only to hold Claude-specific overrides. There are none right now — follow `AGENTS.md`, and for any rental filtering work follow `skills/rental-property-filter/SKILL.md` as `AGENTS.md` directs.

If a future instruction ever needs to apply to Claude Code but not to other agents, add it here and it will take precedence over `AGENTS.md`. Otherwise, put shared rules in `AGENTS.md` so every agent gets them.
